# Copy to .env only if your launcher loads dotenv files, or set these values in
# your shell/hosting provider. The application reads environment variables
# directly and does not require python-dotenv.
ORBIT_SECRET_KEY=replace-with-a-long-random-production-secret
ORBIT_DATABASE=instance/orbit.db
ORBIT_MODEL_DIR=artifacts/models
ORBIT_DEMO_DATA=data/demo/synthetic_orbit_users.csv
ORBIT_SECURE_COOKIES=0
HOST=127.0.0.1
PORT=5000
FLASK_DEBUG=0
